from setuptools import setup

setup(name='ude',
      version='0.1',
      description='Leos Unix Development Environment',
      author='Leonardo Sa',
      packages=['ude'],
      zip_safe=False,
      install_requires=[
        'Jinja2'
      ])
